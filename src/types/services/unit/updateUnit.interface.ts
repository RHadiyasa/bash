export interface IRequestUpdateUnit {
    id: number;
    name: string;
    code: string;
    description?: string;
}

export interface IResponseUpdateUnit {
    id: number;
    name: string;
    code: string;
    description?: string;
}